# The Angular 22 CLI aborts on an unsupported Node version, but pnpm only *warns*
# about a mismatched `engines.node` — so a developer on Node 22.22.2 gets a green
# install and an opaque failure three commands later. `engine-strict` makes the
# install itself the thing that fails, with the supported range in the message.
engine-strict=true
