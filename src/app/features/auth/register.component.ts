import { Component } from '@angular/core';

@Component({
  selector: 'app-register',
  standalone: true,
  template: `<div>Register — coming in next spec item</div>`,
})
export class RegisterComponent {}
